features:
Template definition
Variables
Instantiation
Language safe markers/delimiters
imports
in place variables

todo:
defaults
for loops
inner macros
if
track replacement for updates
place + at + ident

() {} [] , . - : <- testing special character catch -> $#
Hello world

//- include ./examples/example.rs:

/*- def b:

() {} [] , . - :

    pub struct $#struct_name {
        /*- $#var_name -> -*/ str: String
    }
    
    pub impl $#struct_name {
        pub fn new() -> Self{
            Self {
                "Hello world from $#struct_name!".to_string(),
            }
        }
        pub fn $#fn_name(&self){
            println!("{}",self.str);
        }
        $#body
    }

*///- endef:

/*- def c:
    hello $#name from $#origin
*///- endef:

//- place b where struct_name = vec, fn_name=a, body=hello, var_name=str:

//- place b where struct_name = vec2, fn_name=b, body="
        pub fn get_str(&self) -> String {
            return self.str;
        }
//-":

//- def m place c where name = world, origin = france:
//- def d place m where origin = america:
//- place d where origin = brazil:
