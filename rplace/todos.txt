- replacement in the entire project
- place tracking and updating taking into consideration changes
- inherit spaces when placing X
- config X 
- conditional variables if a = b else and defaults 
- variable manipulation by commands-
ex: 
change 
$#var/UPPERFIRST
$#var/UPPERLAST
$#var/LOWEFIRST
$#var/LOWERLAST
$#var/UPPER
$#var/LOWER
$#var/NORMALIZE
trow if not to standard
$#var/CAMEL
$#var/PASCAL
$#var/KEBAB
$#var/SNAKE X
$#var/SCREAMING
chaining
$#var/UPPERFIRST/UPPERLAST

idea:
it would be interesting to allow for custom rules and checks
so you'd be able to define a function to be for example:
//- defrule for var:
:public or static: :vartype type ident: :funcname type ident: ( :repeated :varType type ident: :varName type ident:,: ){
    :body type any:
}
//- endef:
could also be usefull for when 

- vartypes in def
//- def a:
$#var:type any:
$#var:type bool:
//- def a when isTrue = true <- idk how'd I implement that here consistent with the rest 
could be: //- def a when var = val type bool:

when A or B for def
ex: //- def a when A = a or A
ex: //- def a when A/UPPERFIRST = Name:

- inline conditions
- loops / for each in place, def and inline 
ex: for item in list
//- place a were a=[a,b,c]:
- better scope for variables
- global variables
- assertions ex: A = B or else trows
- regex assertions and modifiers ex:
$#var/UPPER:[A-C]
- def as variable
- namespaces
- selective include and alias
- debug / tracing / dump vars / defs 
- inline optionals ex: $#var? if none, then "" 
same for place ex: //- place a where A=B?
- file creation options  
- rules for error output
- sujestions for typos using Levenshtein distance

- improve derive
- add more variable options
- derive the entire folder structure

idea:
id like to be able to create a macro system thats can translate languages
so for example 
//- place func where body="
    let a = b;
    println!("a");
": 
and it would be able to translate to multiple languages X

- make a completion provider for VsCode
- def a derive ...
- place derive

- for loops or iteration over a list
- json integration
- if else
- implement any / _ for match

- array example
//- for (var, type) in collection var:
    $#var+: $#type
//- end:
//- place a where arr=(a,b),(c,d),(e,f), var=val:

- package manager
rplace.toml
ex: 
[project]
name = "name"
version = "version"

[packages]
package1 = {version = "1.0.0.0", features = ["stuffA"]}

- lib.rp file listing all public available templates to be used

package2
- better imports
imports must allow for ./ and not break with the import map so I'd need to store the absolute path

- better inner def scope

- make so it can execute other files

//- parse file.txt: X

- make so that you can parse an entire folder and allow it to handle custom output

- $#var? -> none if not set
- $#var?default
- 
//- if var? = val:
       
//- end
